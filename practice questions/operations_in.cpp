// ~mohd-uzaif-ansari
#include <iostream>
using namespace std;
int main()
{
    int a, b, add, minus, prod, divide, modulu;
    a = 16;
    b = 4;
    add = a + b;
    minus = a - b;
    prod = a * b;
    divide = a / b;
    modulu = a % b;
    cout << "Add = " << (add) << "\n";
    cout << "minus = " << (minus) << "\n";
    cout << "prod = " << (prod) << "\n";
    cout << "divide = " << (divide) << "\n";
    cout << "divide = " << (divide) << "\n";
    cout << "modulu = " << (modulu) << "\n";
    return 0;
}